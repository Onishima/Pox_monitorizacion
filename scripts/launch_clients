#!/bin/bash

SENDER_VAR="sender"
RECEIVER_VAR="receiver"
N_FLOW=$1
LOG=".log"
SENDER=$SENDER_VAR$1$LOG
RECEIVER=$RECEIVER_VAR$1$LOG

~/D-ITG-2.8.1-r1023/bin/ITGSend -T UDP -a 10.0.0.2 -rp 12000 -c 100 -E 100 -t 50000 -l $SENDER &
